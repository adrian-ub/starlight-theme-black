---
import { getNavbarLinks } from '../libs/navbar'

const navLinks = await getNavbarLinks(Astro.currentLocale)
---

<nav class="nav-bar">
  {
    navLinks.map((nav) => (
      <a href={nav.link} {...nav.attrs}>
        {nav.label}
        {nav.badge && <span class="new-dot" aria-hidden="true" />}
      </a>
    ))
  }
</nav>

<style>
  .nav-bar {
    font-size: 0.875rem;
    line-height: 1.25rem;
    align-items: center;
    display: none;
  }

  @media (min-width: 1024px) {
    .nav-bar {
      display: flex;
      gap: 0.375rem;
    }
  }

  .nav-bar a {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-weight: 500;
    min-height: 2rem;
    padding: 0 0.625rem;
    border-radius: calc(var(--radius) - 2px);
    text-decoration: none;
    transition-property: color, background-color, border-color, text-decoration-color, fill, stroke;
    transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
    transition-duration: 0.15s;
    color: var(--foreground);
    position: relative;
    gap: calc(var(--spacing) * 2);
  }

  .nav-bar a:hover {
    color: var(--accent-foreground);
    background-color: color-mix(in oklab, var(--accent) 80%, transparent);
  }

  .new-dot {
    width: calc(var(--spacing) * 2);
    height: calc(var(--spacing) * 2);
    border-radius: 50%;
    background: #3b82f6;
    flex-shrink: 0;
  }
</style>
